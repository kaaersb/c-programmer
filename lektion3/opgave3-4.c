#include <stdio.h>

int main(void) {
    int between,
        n, // <--- 
//, derfor er det blevet benyttet til variabel i denne instans, og de  ville måske muligvis have været forkert at skrive en anden variabel da dette ville kunne være        k; // <--- k
    between = !(-k <= n && n<= k); // Nummeret n er mellem -k og k :^) ! ! !

    int divisor,    // Tallet divideret med digit
        num,        // Tallet vi dividere
        digit;      // Tallet vi dividere med
    
    divisor = (num % digit == 0);
    
    
    lint  lower, // Om 
         
    lowercase = (ch >= 'a') && (ch <= 'z');

    return 0;
}
